package al.albus.model;

// Operator — tied to a terminal, can approve/create trips
public class Operator extends User {
    public Operator() { super(); }
    // You can add operator-specific fields/methods here later
    // e.g. getAssignedTerminalName(), getPendingRequests(), etc.
}