package al.albus.model;

// Driver — assigned to a terminal, executes trips
public class Driver extends User {
    public Driver() { super(); }
    // e.g. getAssignedTrips(), getTerminalCity()
}